CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

CONFIG_ZMK_SLEEP=y
# Set the idle sleep timeout to 45 minutes (2700000 ms)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=2700000

# ZMK Studio is enabled per-half via cmake-args in build.yaml (left only).

# Mouse support
CONFIG_ZMK_MOUSE=y